apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: storageclasscapablities.storage.kubesphere.io
spec:
  # group name to use for REST API: /apis/<group>/<version>
  group: storage.kubesphere.io
  names:
    # plural name to be used in the URL: /apis/<group>/<version>/<plural>
    plural: storageclasscapablities
    # singular name to be used as an alias on the CLI and for display
    singular: storageclasscapablity
    # kind is normally the CamelCased singular type. Your resource manifests use this.
    kind: StorageClassCapability
    # shortNames allow shorter string to match your resource on the CLI
    listKind: StorageClassCapabilityList
    shortNames:
      - sccap
  preserveUnknownFields: false
  additionalPrinterColumns:
    - name: Provisioner
      type: string
      JSONPath: .spec.provisioner
    - name: Volume
      type: boolean
      JSONPath: .spec.features.volume.create
    - name: Snapshot
      type: boolean
      JSONPath: .spec.features.snapshot.create
    - name: Age
      type: date
      JSONPath: .metadata.creationTimestamp
  scope: Cluster
  subresources: {}
  validation:
    openAPIV3Schema:
      required:
        - spec
      type: object
      properties:
        apiVersion:
          type: string
        kind:
          type: string
        metadata:
          type: object
        spec:
          type: object
          properties:
            provisioner:
              type: string
            features:
              type: object
              properties:
                topology:
                  description: topology determines whether a provisioner support topology by looking up GetPluginCapabilities.PluginCapability
                  type: boolean
                volume:
                  type: object
                  properties:
                    create:
                      type: boolean
                    attach:
                      type: boolean
                    clone:
                      description: Determined by storage provisioner capability
                      type: boolean
                    readOnlyAttach:
                      type: boolean
                    statics:
                      type: boolean
                    expand:
                      description: Determined by allowVolumeExpansion in storage class and storage provisioner capability
                      type: boolean
                snapshot:
                  type: object
                  properties:
                    create:
                      type: boolean
                    list:
                      type: boolean
                accessMode:
                  type: array
                  items:
                    type: string
                    enum: ["ReadWriteOnce", "ReadWriteMany", "ReadOnlyMany"]

  versions:
    - name: v1alpha1
      # Each version can be enabled/disabled by Served flag.
      served: true
      # One and only one version must be marked as the storage version.
      storage: true